#WiFi
woori / w15885000

File System 
-> 마운트

1. 윈도우 -> 드라이브 별로 루트가 있슴
2. 리눅스 -> 시스템별 루트(/)는 하나 
                 /dev/hda1 /dev/cdrom 

DAS(USB, 썬더볼트)/NAS(랜케이블)/SAN(광케이블)

cf.  WMWare ESXi+vCenter = vSphere

3. HDFS(하둡파일시스템)
여러 대를 묶어서 루트(/)로 묶는 가상파일시스템

HDFS 블럭 - 64MB
메모리-페이지(4KB)/디스크-클러스터(4KB)

HDFS over NTFS / exFAT / ext4fs / HPFS
SAMBA(SMB) / Paragon NTFS



